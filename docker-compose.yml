services:
  crm:
    build: .
    container_name: crm-abj
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://crm_dev:CrmAbj2026Secure@host.docker.internal:5432/crm_abj_dev
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
      - NEXTAUTH_URL=${NEXTAUTH_URL}
      - NOTIFICATIONS_API_KEY=dev-key
      - N8N_WEBHOOK_URL=http://host.docker.internal:5678/webhook
      - N8N_API_KEY=n8n-abj-secret-2026
    extra_hosts:
      - "host.docker.internal:host-gateway"
